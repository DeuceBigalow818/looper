{
    "name": "loop-executor",
    "version": "1.0.0",
    "description": "LoopExecutor — One-click leveraged staking on OPNet via Stash + MOTO",
    "type": "module",
    "scripts": {
        "build": "asc src/index.ts --config asconfig.json --target debug",
        "build:release": "asc src/index.ts --config asconfig.json --target release",
        "build:mocks": "cd mocks/mock-stash && npm run build && cd ../mock-moto && npm run build",
        "build:all": "npm run build && npm run build:mocks",
        "test": "npx tsx test/loopExecutor.test.ts",
        "deploy:regtest": "npx tsx scripts/deploy-regtest.ts",
        "clean": "rm -rf build/*"
    },
    "dependencies": {
        "@btc-vision/as-bignum": "0.1.2",
        "@btc-vision/btc-runtime": "rc"
    },
    "devDependencies": {
        "@btc-vision/assemblyscript": "^0.29.2",
        "@btc-vision/opnet-transform": "1.1.0",
        "@assemblyscript/loader": "latest"
    },
    "overrides": {
        "@noble/hashes": "2.0.1"
    }
}
